---
- name: archlinux | install
  pacman: 
    name: "{{ shell_tools.pacman_packages }}"
    state: present
  become: true

- name: archlinux | yay | clone repository
  git:
    repo: "https://aur.archlinux.org/yay.git"
    dest: "/tmp/yay"

- name: archlinux | yay | install deps
  become: true
  pacman:
    name: base-devel
    state: present

- name: archlinux | yay | install
  command:
    cmd: "makepkg -si --noconfirm"
    chdir: "/tmp/yay"
  