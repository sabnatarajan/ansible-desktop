---
gpg_homebrew_packages:
  - gnupg
  # - hopenpgp-tools
  - pinentry-mac

gpg_config_link: true
gpg_config_src_mac: $HOME/git/dotfiles/gnupg/gpg-agent-mac.conf
gpg_config_src_linux: $HOME/git/dotfiles/gnupg/gpg-agent-linux.conf
gpg_config_dst: $HOME/.gnupg/gpg-agent.conf

gpg_import_keys:
  - 7561E764BECC583B
